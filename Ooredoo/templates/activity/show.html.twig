{% extends 'dashboard/index.html.twig' %}

{% block title %}Activity{% endblock %}

{% block bodyx %}
<div class="wrapper">
    <div class="sa4d25">
        <div class="container" style="margin-left: 5%">
            {% block cssx %}
            <link href="{{ asset('css/form2.css') }}" rel="stylesheet">
            {% endblock %}<!-- css endblock -->
    <h1>Activity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ChangeId</th>
                <td>{{ activity.ChangeId }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ activity.Title }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ activity.Date ? activity.Date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ activity.Status ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Owner</th>
                <td>{{ activity.Owner }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_activity_index') }}">back to list</a>

    <a href="{{ path('app_activity_edit', {'ChangeId': activity.ChangeId}) }}">edit</a>

    {{ include('activity/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
